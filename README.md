## Local setup

1. Install dependencies with `pnpm install` (run this in each new git worktree).
2. Copy `.env.example` to your local env file (for example, `.env.local`).
3. Fill in all required variables with real values.
4. Keep advanced mode disabled (`ENABLE_CODEX_APP_SERVER=false`) unless you are testing the Codex app-server.
