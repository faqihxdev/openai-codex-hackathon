## Local setup

1. Copy `.env.example` to your local env file (for example, `.env.local`).
2. Fill in all required variables with real values.
3. Keep advanced mode disabled (`ENABLE_CODEX_APP_SERVER=false`) unless you are testing the Codex app-server.
